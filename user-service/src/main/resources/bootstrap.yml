---
spring:
  banner-mode: off
  application:
    name: user-service
  cloud:
    config:
      enabled: true
      uri: http://localhost:8081
      name: config-service
  profiles:
    active: default
...